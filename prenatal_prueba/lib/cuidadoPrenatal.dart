import 'package:flutter/material.dart';

class CuidadoPrenatal extends StatelessWidget {
  const CuidadoPrenatal({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cuidado Prenatal'),
      ),
      body: const Center(
        child: Text('Cuidado Prenatal'),
      ),
    );
  }
  }
